extends layout

block content

  link(rel='stylesheet' href='/bower_components/datatables/media/css/jquery.dataTables.min.css')
  .container
    h2(align='center') Detailed view
    .container
      table#detailedview.display(style='width:100%')
        thead
          th Parameter
          th Value
        tbody
          tr
            th date:
            td #{downtimeevent.date}
          tr
            th time:
            td #{downtimeevent.time}
          tr
            th Machine:
            td #{downtimeevent.machine}
          tr
            th code:
            td #{downtimeevent.code}
          tr
            th duration:
            td #{downtimeevent.duration} minutes
          tr
            th Author:
            td #{downtimeevent.operator}
          tr
            th description:
            td #{downtimeevent.description}
          


    hr
    .ifLoggedIn
      a.btn.btn-warning(href='/downtimeevent/edit/'+downtimeevent._id) Edit
      if (downtimeevent.archived == false)
        a.btn.btn-danger(href='/downtimeevent/archive/'+downtimeevent._id) Archive
      if (downtimeevent.archived == true)
        a.btn.btn-danger(href='/downtimeevent/resurrect/'+downtimeevent._id) Resurrect

  script(src='/bower_components/jquery/dist/jquery.js')
  script(src='/bower_components/datatables/media/js/jquery.dataTables.min.js')
  script.
    $(document).ready( function () {
    $('#detailedview').DataTable({
    "bFilter": false,
    "bInfo" : false,
    "bPaginate": false,
    "bSort" : false
    });
    } );
